# Only the optional AI workflow reads these environment variables.
# This file is a template; Python does not load .env files automatically.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
